# Production environment variables for MongoDB
# IMPORTANT: Replace <root> with your actual MongoDB password before deploying

# MongoDB Connection (MongoDB Atlas)
MONGODB_URI=mongodb+srv://root:<root>@assistweb.u6rpliy.mongodb.net/attendance_system?retryWrites=true&w=majority&appName=assistweb

# Alternative local MongoDB (for development)
# MONGODB_URI=mongodb://localhost:27017/attendance_system

# JWT Configuration
# Generate a strong random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secure-jwt-secret-key-change-this-in-production
JWT_EXPIRES_IN=30d
JWT_REFRESH_EXPIRES_IN=90d

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (Vercel deployment URL)
FRONTEND_URL=https://assist-web.vercel.app

# NOTE: SSL certificates are not needed for Vercel deployment
# Vercel automatically provides HTTPS
